{
  "name": "serverless-image-processor",
  "version": "0.0.1",
  "dependencies": {
    "@google-cloud/storage": "^2.3.1",
    "axios": "^0.18.0"
  },
  "scripts": {
    "deploy": "gcloud functions deploy serverless-image-processor --project=serverless-image-processor --entry-point=helloWorld --region=europe-west1 --trigger-http --runtime=nodejs8 --memory=128"
  }
}
